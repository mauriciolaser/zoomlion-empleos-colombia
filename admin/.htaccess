# /empleos/admin/.htaccess

# 1. Evitar listado de directorios
Options -Indexes

# 2. Denegar acceso directo a cualquier archivo dentro de /acciones
RewriteEngine On
RewriteRule ^acciones/ - [F]

# 3. Reescribir todas las demás peticiones a index.php
RewriteCond %{REQUEST_FILENAME} !-f
RewriteCond %{REQUEST_FILENAME} !-d
RewriteRule ^.*$ index.php [L,QSA]
